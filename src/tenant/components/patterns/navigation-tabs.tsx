import type { ReactNode } from 'react';
import { cn } from '../../lib/utils';

export function TabBar<T extends string>({ value, onChange, tabs, className }: { value: T; onChange: (value: T) => void; tabs: Array<{ value: T; label: ReactNode; count?: number; icon?: ReactNode }>; className?: string }) {
  return <div className={cn('flex items-center gap-1 overflow-x-auto border-b border-border', className)} role="tablist">{tabs.map((tab) => { const active = tab.value === value; return <button key={tab.value} type="button" role="tab" tabIndex={active ? 0 : -1} onKeyDown={(event) => { const index = tabs.findIndex((candidate) => candidate.value === tab.value); const next = event.key === 'ArrowRight' ? (index + 1) % tabs.length : event.key === 'ArrowLeft' ? (index - 1 + tabs.length) % tabs.length : event.key === 'Home' ? 0 : event.key === 'End' ? tabs.length - 1 : -1; if (next >= 0) { event.preventDefault(); onChange(tabs[next].value); (event.currentTarget.parentElement?.querySelectorAll('button')[next] as HTMLButtonElement)?.focus(); } }} aria-selected={active} onClick={() => onChange(tab.value)} className={cn('relative inline-flex items-center gap-2 whitespace-nowrap px-3.5 py-2.5 text-[13px] font-medium transition-colors', active ? 'text-foreground' : 'text-subtle hover:text-muted')}>{tab.icon}{tab.label}{tab.count !== undefined && <span className={cn('tnum rounded px-1.5 text-[11px]', active ? 'bg-primary-muted text-primary' : 'bg-surface-inset text-subtle')}>{tab.count}</span>}{active && <span className="absolute inset-x-2 -bottom-px h-[2px] rounded bg-primary" />}</button>; })}</div>;
}
